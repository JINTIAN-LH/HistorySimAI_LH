# Session Impact Report（相对未更新前主工程）

## 1. 结论摘要
本次 session 已从“SDD 刚性版主链路接入”推进到“困难模式完整闭环验收”。核心影响是：
- 新增 `rigid_v1` 玩法主线，并完成经典主链路复用、困难规则插件层、叙事双轨、验收报告链路四个层面的闭环。
- 保留原工程主体结构，困难模式未复制经典主流程，而是复用 `turnSystem + storySystem` 主干，在规则层注入约束。
- 模式切换、存档隔离、LLM/fallback 兼容、结构化验收证据均已落地，可继续进入实机平衡阶段。

## 2. 变更范围

### 2.1 修改的既有文件（对主工程直接影响）
- `css/theme.css`
- `data/config.json`
- `data/rigidTriggers.json`
- `js/main.js`
- `js/state.js`
- `js/api/requestContext.js`
- `js/systems/storySystem.js` (新增：中文数字解析、故事线关闭)
- `js/systems/turnSystem.js` (新增：困难模式敌对势力解决、记忆锚点)
- `js/utils/appointmentEffects.js` (新增：赐死官员检测、characterDeath effects)
- `js/rigid/config.js`
- `js/rigid/decisionCheck.js`
- `js/rigid/engine.js`
- `js/rigid/mechanisms.js`
- `js/rigid/moduleComposer.js`
- `js/rigid/memory.js` (新增：敌对势力灭亡记忆锚点)

### 2.2 新增文件/目录（增量能力）
- `js/rigid/`
  - `config.js`
  - `valueCheck.js`
  - `mechanisms.js`
  - `decisionCheck.js`
  - `history.js`
  - `memory.js`
  - `moduleComposer.js`
  - `engine.js`
  - `engine.test.js`
  - `moduleComposer.test.js`
- `js/systems/turnSystem.pipeline.test.js`
- `js/api/requestContext.test.js`
- `js/utils/appointmentEffects.test.js` (新增：赐死、组合效果测试)
- `data/rigidInitialState.json`
- `data/rigidTriggers.json`
- `data/rigidHistoryEvents.json`
- `HARD_MODE_ACCEPTANCE_REPORT.md`
- `SDD_CHECKLIST_TRACKER.md`
- `SSD_MIGRATION_REPORT.md`

## 3. 功能行为影响

### 3.1 启动与模式
- 影响：`config.json` 增加 `gameplayMode`，默认改为 `classic`。
- 结果：首启由玩家选择 `classic/rigid_v1`，并按模式加载独立存档。
- 兼容：保留模式判断，后续可切回旧模式（配置层可控）。

### 3.2 状态模型
- 影响：`state.js` 新增 `mode` 与 `rigid` 子状态树。
- 结果：原状态未移除；新增刚性维度（财政/军事/朝廷/圣躬/其他、记忆锚点、事件队列等）。
- 风险：存档体积增加（`memoryAnchors` 累积，已做截断上限）。

### 3.3 回合主循环
- 影响：`turnSystem.js` 增加 rigid 分支执行器。
- 结果：在 rigid 模式下执行“校验→折扣/封驳→反弹→阈值触发→8模块→推进3个月→存档锚点”闭环。
- 新增：
  - 六科封驳“每回合至少 1 次”刚性兜底；
  - 罢朝分级（1/2/3）与复朝处置；
  - 困难专属结算入口复用统一差分面板。
- 兼容：非 rigid 模式仍走原逻辑分支。

### 3.4 叙事与交互
- 影响：`storySystem.js` 增加 rigid 渲染分支与提交锁。
- 结果：
  - 玩家侧继续看到自然叙事，不直接暴露内部模块；
  - 系统侧保留 `rigidMeta` 结构化数据，用于测试、回放与 LLM 上下文；
  - 罢朝时默认禁用施政，但允许“复朝处置”应急选项；  - **故事线关闭**（新增 2026-03-27）：敌对势力灭亡时自动关闭相关故事线标签，后续背景渲染不再涉及已灭亡的势力；  - 历史分支与暗杀后续为强制处理门控；
  - 防重复提交（按钮锁）。

### 3.5 文案与体验
- 影响：`moduleComposer.js` 与 `rigidHistoryEvents.json` 深化历史叙事。
- 结果：
  - 10历史节点按 1627-1644 时序细化；
  - 模块2固定8项叙事子段；
  - 模块3固定六部+厂卫+军情，并强制输出“信息茧房矛盾”；
  - 引入“悲剧脉冲”动态文案（随阻力/焦虑/暗杀风险变化）。
  - LLM 请求体增加 `latestMemoryAnchor` 与 `lastOutputModules`，帮助困难叙事稳定贴近系统状态。

### 3.6 移动端可读性
- 影响：`theme.css` 增加小屏样式兜底。
- 结果：诏书文本和决策按钮在小屏下可读性更好。
- 备注：仍建议真机验收（仅完成样式级兜底，不含设备实测记录）。

### 3.7 官员管理与赐死机制（2026-03-27 新增）
- 影响：`js/utils/appointmentEffects.js` 新增官员死亡检测。
- 变化：
  - 从圣旨/决策文本中识别赐死关键词（赐死、赐予自尽、饮鸩等）
  - 生成 `characterDeath` effects，标记官员 `isAlive: false`
  - 关闭该官员相关的故事线标签
- 兼容：任免、罢免、赐死逻辑统一在 `deriveAppointmentEffectsFromText()`，两种模式均调用同一 `applyEffects()`

### 3.8 敌对势力灭亡与故事线关闭（2026-03-27 新增）
- 影响：`resolveHostileForcesAfterChoice()` 在困难模式解决敌对势力更新
- 变化：
  - 困难模式现在检测敌对势力灭亡事件，生成记忆锚点
  - 自动设置 `closedStorylines[tag] = true`，后续背景渲染不再涉及该势力
  - 数据持久化到 `localStorage`（plain JSON，无压缩）
- 兼容：两种模式共享故事线关闭逻辑，经典模式已此机制已验证并正常工作

### 3.9 中文数字诏书解析（2026-03-27 新增）
- 影响：`storySystem.js` 的 `parseExplicitResourceDelta()` 增强。
- 变化：
  - Pattern 1：支持 "八万两" → 80000
  - Pattern 2：支持纯中文 "五千石" → 5000 等
  - 句级推断：当前缀符号含糊时（如"虽损八万但……"），用整句主意图确定加减
- 兼容：失败时回退默认行为，不破坏已有圣旨模板

## 4. 规则与数值影响
- 新增硬下限、自动反弹、阈值触发、执行折扣、性格惩罚、状态联动。
- 历史事件支持强制触发 + 分支选择（不可跳过）。
- 暗杀事件支持后续处置（追查/压案）且不返回主谋。
- 罢朝机制已升级为分级状态机，支持 1/2/3 级触发、降级恢复、复朝释放。
- 信息茧房已从“概念约束”变为“每回合固定输出一处六部 vs 厂卫矛盾”。
- 六科封驳已从“概率性结果”提升为“每回合至少增加 1 次”的刚性兜底。
- 决策时间推进由刚性分支固定为每回合 +3 月。

## 5. 质量与测试影响
- 测试新增：`js/rigid/engine.test.js`、`js/rigid/moduleComposer.test.js`、`js/systems/turnSystem.pipeline.test.js`、`js/api/requestContext.test.js`、`js/utils/appointmentEffects.test.js`。
- 当前已验证的最终验收集：
  - `npm test -- js/rigid/engine.test.js js/rigid/moduleComposer.test.js js/systems/turnSystem.pipeline.test.js js/api/requestContext.test.js js/utils/appointmentEffects.test.js`
  - 结果：5 个测试文件通过，**169 个测试通过**。
- 新增覆盖（2026-03-27）：
  - 中文数字诏书解析 (Pattern 1 & 2、句级推断)
  - 敌对势力灭亡 → 记忆锚点生成 → 故事线关闭流程
  - 字符赐死检测 (0-2字邻近匹配)、characterDeath state 应用
  - 组合效果处理 (任免+罢免+赐死混合圣旨)
- 说明：当前通过的是困难模式主链路、经典模式任免赐死、敌对势力解决等完整链路测试，覆盖 169 个测试用例，0 回归。

## 6. 回滚与开关建议
- 若需快速回到旧体验：
  1. 在设置页切换回 `classic`；
  2. 保留 `js/rigid/*` 文件不删除（便于灰度切换）；
  3. 如需彻底回退，清理对应模式存档后重启。

## 7. 对主工程的总体判断
- 架构层面：增量扩展，未重构主框架，且已完成“经典主链路 + 困难规则插件层”的边界收口。
- 风险层面：中低风险；当前主要剩余风险不在架构，而在“在线 LLM 联调稳定性”和“长期平衡参数调优”。
- 维护层面：刚性逻辑集中在 `js/rigid/`，验收证据集中在设计文档、测试与独立验收报告，后续维护成本可控。

## 8. 经典模式链路总览（玩法 / UI / 模块）

### 8.1 玩法主链路（Classic）
- 经典模式本质是“默认主链路”：除 `isRigidMode(state)` 命中时走刚性分支外，均走经典分支。
- 启动加载入口：`js/main.js`
  - `preloadBasicData(preferredMode)` 统一装载 config、characters、factions、goals、nationInit、positions，并初始化 core gameplay 状态。
  - `selectedMode = current.mode || preferredMode || config.gameplayMode || "classic"`。
  - `bootstrap()` 内按是否 `gameStarted` 进入 start 或 edict。
- 回合入口：`js/ui/edictView.js`
  - `runCurrentTurn(container, { renderId })` 进入 `js/systems/turnSystem.js`。
- 经典回合推进：`js/systems/turnSystem.js` -> `handleChoice()`（非 rigid 分支）
  1. 解析并归一化本回合 effects（含自拟诏书关键词估算、任免文本推断）。
  2. `pushCurrentTurnToHistory` 写入历史。
  3. 推进时间（月/年/日）。
  4. `processCoreGameplayTurn` 执行核心玩法结算。
  5. `resolveHostileForcesAfterChoice` 执行敌对势力后处理。
  6. 推进科举/武举与自然死亡。
  7. 季度月触发 `computeQuarterlyEffects` 并生成季度结算快照。
  8. `refreshQuarterAgendaByState` 刷新季度议题。
  9. 构建显示差分 `buildOutcomeDisplayDelta`，自动保存并重渲染。

### 8.2 剧情驱动链路（Classic）
- 渲染入口：`js/systems/storySystem.js` -> `renderStoryTurn(state, container, onChoice, options)`。
- 当前回合数据加载：`loadStoryData()`
  - 条件：`storyMode === "llm" && apiBase 非空 && 非首回合` 时请求 LLM。
  - LLM 请求：`requestStoryTurn()`（`js/api/llmStory.js`）。
  - 失败回退：本地 `data/story/*.json` 模板。
- 页面结构：
  - 历史回合块 `renderStoryHistory`。
  - 当前回合块 `renderCurrentTurn`。
  - 季度奏折面板 `renderQuarterAgendaPanel`。
  - 选项按钮 + 自拟诏书弹层 + 数值变化卡片 + 标注系统。

### 8.3 UI 结构映射
- 路由与导航：`js/router.js`
  - View：`start / edict / court / nation / settings`。
  - 切换时保存 edict 滚动位置（`saveEdictScrollTop`）。
- 顶栏与底栏：`js/layout.js`
  - `initLayout` 创建 topbar/bottombar。
  - `updateTopbarByState` 显示“崇祯X年·X月·时段·天气”。
  - `updateGoalBar` 与 `updateMinisterTabBadge` 联动目标/未读。
- 诏书页：`js/ui/edictView.js`
  - 负责触发回合渲染，不承载具体业务规则。
- 朝堂页：`js/ui/courtView.js`
  - 官员列表、聊天、忠诚标记、任命弹窗。
  - 科举/武举弹层（人才池、放榜、录用）。
  - 对话后调用统一差分卡展示“本轮对话数值变化”。
- 国家页：`js/ui/nationView.js`
  - 国家指标、朝局指标、派系支持。
  - 季度奏折、能力加点、国策树解锁、自定义国策。
  - 省份概况、敌对势力、天下大事、民间舆论。

### 8.4 核心模块职责拆分
- 状态与存档
  - `js/state.js`：全局状态树与派生 ministers 逻辑。
  - `js/storage.js`：按模式分离存档键（classic/rigid）。
- 核心玩法
  - `js/systems/coreGameplaySystem.js`：
    - 执行率、国策树、能力加点。
    - 自定义国策提取与季度加成。
    - 季度议题生成、敌对势力结算。
    - 主回合聚合 `processCoreGameplayTurn`。
- 剧情与 API
  - `js/api/requestContext.js`：组装 story/chat 请求体。
  - `js/api/llmStory.js`：剧情请求、容错解析、header 对齐。
  - `js/api/ministerChat.js`：朝臣对话请求。
  - `js/api/validators.js`：effects 限幅与 payload 归一化。
- 数值显示与效果应用
  - `js/utils/effectsProcessor.js`：nation/loyalty 数值写入。
  - `js/utils/displayStateMetrics.js`：
    - 指标定义、快照、差分、合并与卡片渲染。
    - 诏书页、朝堂页、国家页共享。

### 8.5 配置与数据驱动
- 全局配置：`data/config.json`
  - 当前默认：`storyMode: "llm"`、`gameplayMode: "classic"`、`autoSave: true`。
- 初始数据：
  - `data/characters.json`、`data/factions.json`、`data/goals.json`、`data/nationInit.json`、`data/positions.json`。
- 剧情模板：`data/story/*.json`（LLM 不可用时回退）。

### 8.6 经典模式关键文件清单（按层）
- 入口层：`js/main.js`、`js/router.js`、`js/layout.js`
- 玩法层：`js/systems/turnSystem.js`、`js/systems/storySystem.js`、`js/systems/coreGameplaySystem.js`
- 页面层：`js/ui/edictView.js`、`js/ui/courtView.js`、`js/ui/nationView.js`、`js/ui/startView.js`、`js/ui/settingsView.js`
- API层：`js/api/llmStory.js`、`js/api/ministerChat.js`、`js/api/requestContext.js`、`js/api/validators.js`、`js/api/httpClient.js`
- 基础层：`js/state.js`、`js/storage.js`、`js/dataLoader.js`、`js/utils/effectsProcessor.js`、`js/utils/displayStateMetrics.js`

### 8.7 一句话结论
- 经典模式是主工程默认主干：它通过 `turnSystem + storySystem + coreGameplaySystem` 形成“叙事驱动 + 数值结算 + 季度治理 + 人事互动”的闭环；UI 仅负责展示与交互触发，规则在 systems 层集中，API 与 validators 提供稳定输入输出边界。

### 8.8 任免与官员管理流程（经典 & 困难 统一）
- 流程入口：`js/utils/appointmentEffects.js` -> `deriveAppointmentEffectsFromText()`
  - 从圣旨/决策文本中提取：任命、罢免、赐死三类事件
  - 支持中文数字金额解析（如"八万两"、"五千石"）
  - 返回统一的 effects 对象：`{ appointments, appointmentDismissals, characterDeath }`
- 关键词与模式：
  - **任命**：`任命|拜|授为|提拔` 匹配 "官位名|职位" 语境
  - **罢免**：`免去|罢免|撤销|去职` 匹配 "当前职位" 或 "官员无职位上下文"
  - **赐死**：`赐死|赐予自尽|自尽|饮鸩|毒酒` 匹配 "官员名"（0-2字邻近性确保精准）
- 状态应用：`js/systems/storySystem.js` -> `applyEffects()`（两种模式共享）
  - 任命/罢免：更新 `characterStatus[characterId].positions` 与 `loyalty`
  - **赐死**：标记 `characterStatus[characterId] = { isAlive: false, deathReason, deathDay }`，关闭相关故事线
  - 两种模式均在 `turnSystem.js` handleChoice 后统一调用此函数
- 测试覆盖：`js/utils/appointmentEffects.test.js` (7/7 通过)

## 9. 敌对势力与故事线闭锁机制（经典 & 困难 统一）

### 9.1 敌对势力后处理流程
- 流程入口：`resolveHostileForcesAfterChoice()` (在 `js/systems/turnSystem.js` 的 handleChoice 中调用，两种模式均触发)
- 执行步骤：
  1. 遍历当前敌对势力列表
  2. 计算势力衰减、判定灭亡条件
  3. **新灭亡势力**：生成记忆锚点 + 关闭关联故事线 + 更新 news
- 新增机制（2026-03-27）：
  - **困难模式记忆锚点**：`appendMemoryAnchor({ type: "hostileDefeat", characterId, summary })` 
  - **故事线关闭**：设置 `state.closedStorylines[tag] = true`，在后续背景渲染时不再生成该标签相关的故事
  - **本地持久化**：`localStorage` 计位 plain JSON stringify（无压缩）

### 9.2 关键代码位置
- `js/systems/turnSystem.js` (lines 302-330)：困难模式新增敌对势力解决逻辑
- `js/rigid/memory.js`：`appendMemoryAnchor()` 记忆锚点生成与存储
- `js/systems/storySystem.js` -> `applyEffects()`：故事线关闭逻辑（两模式共享）

### 9.3 诏书文案解析增强（2026-03-27）

**功能**：支持圣旨中的中文数字金额自动解析

- 解析位置：`js/systems/storySystem.js` -> `parseExplicitResourceDelta()` (lines 843-905)
- 支持模式：
  - **Pattern 1** (Arabic+万)：`^([-+]?)(\d+)万两?$` → 金额 × 10000 = 两
  - **Pattern 2** (纯中文)：`^([-+]?)([一二三四五六七八九十百千万]+?)(?:两|石)?$` → 转换为数值后按单位扩展
- 处理示例：
  - `"+八万两"` → +80000
  - `"-五千石"` → -5000
  - `"加三百万"` → +3000000
- 递推规则：`parseCnAmount("八万五千二百")` → 85200
- 句级符号推断：当前缀含糊时，取整句主意图确定加减符号（如"虽损八万但……"识别为负）
- 测试验证：`turnSystem.pipeline.test.js` 已验证与困难模式选择后敌对势力更新协同

## 10. 困难模式验收链路（2026-03-27）

- 已建立独立验收报告：`HARD_MODE_ACCEPTANCE_REPORT.md`
- 已完成阶段 0-4：主链路稳定、规则插件层、叙事双轨、测试门禁、验收汇总。
- 当前自动化验收命令：
  - `npm test -- js/rigid/engine.test.js js/rigid/moduleComposer.test.js js/systems/turnSystem.pipeline.test.js js/api/requestContext.test.js appointmentEffects.test.js`
- 当前自动化结果：5 个测试文件通过，**169 个测试通过**（新增赐死、故事线关闭、敌对势力解决等4个测试）。
- 新增覆盖项：
  - ✅ 中文数字金额解析（edict parsing）
  - ✅ 敌对势力灭亡 → 记忆锚点 → 故事线关闭流程
  - ✅ 赐死官员检测 & 状态更新（两模式共用）
  - ✅ 组合效果处理（任免+赐死混合圣旨）

## 11. 当前最终态（一句话）

- 相对未更新前主工程，当前仓库已具备"经典模式主干不被破坏、困难模式规则可插拔、**任免赐死官员死亡统一**、**敌对势力灭亡自动关闭故事线**、**中文数字诏书自动解析**、叙事与结构化数据双轨、自动化验收可追踪（169 tests）"的完整困难模式实现形态。

